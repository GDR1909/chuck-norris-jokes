/** 
 * @type {import('postcss-load-config').Config}
 * 
 * This defines the type of configuration using TypeScript to provide type checking and IntelliSense.
 */
const config = {
  plugins: {
    tailwindcss: {},
  },
};

export default config;